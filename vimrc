syntax on
set number

set nocompatible
filetype plugin on

let g:vimwiki_list = [{'path': '$HOME/Dropbox/Apps/KiwiApp/wiki'}] " set path to a directory inside Dropbox
let g:vimwiki_ext = '.md' " set extension to .md
let g:vimwiki_global_ext = 0 " make sure vimwiki doesn't own all .md files
